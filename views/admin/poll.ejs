<!DOCTYPE html>
<html>
<head>
  <title>Administration - Sondage en cours</title>
  <link rel='stylesheet' href='/stylesheets/bootstrap.css'/>
  <link rel='stylesheet' href='/stylesheets/bootstrap-theme.css'/>
  <link rel='stylesheet' href='/stylesheets/style.css'/>
  <script src="../js/jquery.js"></script>
  <script src="../js/jquery-ui.js"></script>
</head>
<body>
<nav class="navbar navbar-default">
  <div class="container-fluid">
    <a class="navbar-brand" href="#">Sondage en cours</a>

    <!-- Collect the nav links, forms, and other content for toggling -->
    <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
      <ul class="nav navbar-nav">
        <li><a href="/admin">Retour à l'accueil de la partie administration</a></li>
      </ul>
    </div>
    <!-- /.navbar-collapse -->
  </div>
  <!-- /.container-fluid -->
</nav>
<main class="container">
  <div class="contentDiv col-md-3">
  </div>
  <div class="contentDiv col-md-6">
    <% if(respondents) {
    if (respondents.length) { %>
    <p>Présents : <%= presentCount %>, Absents : <%= absentCount %>, Sans réponse : <%= noResponseCount %>.</p>
    <table class="table table-striped  table-hover table-condensed">
      <thead>
      <tr>
        <th width="5%"></th>
        <th width="20%">Sondé</th>
        <th width="20%">Réponse</th>
        <th width="20%">Date</th>
      </tr>
      </thead>
      <tbody>
      <% respondents.forEach(function(item) { %>
      <tr>
        <td><button class="resendPollButton btn btn-default btn-xs" guid="<%= item.guid %>" type="button"
                    title="Renvoyer le sondage">
            <span class="glyphicon glyphicon-send" aria-hidden="true"></span>
          </button></td>
        <td><%= item.name %></td>
        <% if(item.status != null) {
        if(item.status) { %>
        <td>Oui</td>
        <% }  else { %>
        <td>Non</td>
        <% }
        } else { %>
        <td>-</td>
        <% } %>
        <td><%= item.dateText %></td>
      </tr>
      <% }); %>
      </tbody>
    </table>
    <% } else { %>
    <p>Pas de sondé dans le sondage ouvert actuellement. Il y a peut-être un problème...</p>
    <% }} else { %>
    <p>Pas de sondage ouvert actuellement.</p>
    <% } %>
  </div>
  <div class="contentDiv col-md-3">
  </div>
</main>
<script src="/js/source.js"></script>
</body>
</html>